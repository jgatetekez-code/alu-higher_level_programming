This project explores one of Python's core philosophies: **everything
is an object**. Numbers, strings, lists, functions, classes, and even
modules are all objects in Python, each with a type, an identity, and
a value.
