This project contains a Python function called safe_print_list that safely prints a specified number of elements from a list.

The function uses try  and except to handle cases where the requested number of elements is greater than the number of items in the list. Instead of causing an error, it prints only the available elements and returns the actual number of elements printed.

